---
title: WorkspacePolicy
description: "WorkspacePolicy CRD reference (waas.xorhub.io/v1alpha1), generated from the waas JSON Schemas."
sidebar_position: 4
---

{/* GENERATED by scripts/generate-crd-docs.mjs from crd-schemas/waas.xorhub.io/workspacepolicy_v1alpha1.json
    (waas@03c1445d80c1) — DO NOT EDIT. Refresh: scripts/sync-crd-schemas.mjs */}

:::info Generated reference
This page is generated at build time from the [`crd-schemas`](https://github.com/XoRHub/waas/tree/main/crd-schemas) JSON Schemas published by the waas repository, vendored at ref `03c1445d80c1`.
:::
# WorkspacePolicy

WorkspacePolicy is the self-service envelope for a user or group.

| | |
| --- | --- |
| **apiVersion** | `waas.xorhub.io/v1alpha1` |
| **kind** | `WorkspacePolicy` |

## spec

WorkspacePolicySpec is the admin-defined envelope for self-service.

Resolution rule (deliberate, keep in sync with pkg/policy): among all
policies whose subjects match the user, the HIGHEST spec.priority wins
and applies as a whole — no field merging between policies. Ties break
on the lexicographically smallest name and surface a warning, because
two same-priority matches are a configuration smell. A policy with no
subjects matches every authenticated user: name one "default" with
priority 0 as the restrictive fallback. No matching policy at all
means DENY (fail closed).

| Field | Type | Description |
| --- | --- | --- |
| <span className="crd-field-path">`images`</span> | []string | Images is the subset of catalog entries (WorkspaceImage names) this policy allows. Empty = the whole enabled catalog (each image's own allowedGroups still applies). |
| <span className="crd-field-path">`priority`</span> | integer (int32) | Priority orders competing policies; higher wins. Convention: 0 = default fallback, 100–999 = group policies, 1000+ = per-user exceptions. Minimum: `0`. |
| <span className="crd-field-path">`remoteWorkspaces`</span> | boolean | RemoteWorkspaces opts the governed users into the Remote Workspaces feature: registering out-of-cluster machines (host/port/protocol + credentials Secret) and connecting to them through guacd. Absent or false = feature hidden and refused (fail closed). |

### spec.clipboard

Clipboard controls the clipboard bridge between the user's machine
and their workspace sessions. Absent = both directions allowed.
Enforced by the WebSocket proxy (instruction filtering, rights
stamped into the connection token) — the portal only reflects it.

| Field | Type | Description |
| --- | --- | --- |
| <span className="crd-field-path">`clipboard`</span> | object |  |
| <span className="crd-field-path">&numsp;&numsp;`copyFromWorkspace`</span> | boolean | CopyFromWorkspace permits copying FROM the workspace to the local clipboard (data exfiltration direction). |
| <span className="crd-field-path">&numsp;&numsp;`pasteToWorkspace`</span> | boolean | PasteToWorkspace permits pasting the local clipboard INTO the workspace (data injection direction). |

### spec.lifecycle

Lifecycle bounds workspace longevity to fight accumulation.

| Field | Type | Description |
| --- | --- | --- |
| <span className="crd-field-path">`lifecycle`</span> | object |  |
| <span className="crd-field-path">&numsp;&numsp;`idleSuspendAfter`</span> | string | IdleSuspendAfter pauses a workspace with no active session for this long (compute freed, home kept). Zero/absent = never. |
| <span className="crd-field-path">&numsp;&numsp;`maxLifetime`</span> | string | MaxLifetime deletes the workspace (home included) this long after creation. Zero/absent = never. Deletion is announced through the workspace's Events and status conditions. |

### spec.limits

Limits caps what the matched user can consume.

| Field | Type | Description |
| --- | --- | --- |
| <span className="crd-field-path">`limits`</span> | object |  |
| <span className="crd-field-path">&numsp;&numsp;`aggregate`</span> | object | Aggregate caps the SUM across all of the user's workspaces (paused included for storage; compute of paused workspaces is released and therefore not counted). |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`cpu`</span> | int-or-string | Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`memory`</span> | int-or-string | Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`storage`</span> | int-or-string | Storage caps the sum of home volume sizes. Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |
| <span className="crd-field-path">&numsp;&numsp;`defaults`</span> | object | Defaults is the sizing the portal proposes when the user does not choose. Display-only: a per-image default (image.resources.default) takes precedence, and enforcement is unaffected either way. |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`cpu`</span> | int-or-string | Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`memory`</span> | int-or-string | Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |
| <span className="crd-field-path">&numsp;&numsp;`maxWorkspaces`</span> | integer (int32) | MaxWorkspaces is the max simultaneous workspaces per user. Paused workspaces count: their home PVC still holds storage. Minimum: `0`. |
| <span className="crd-field-path">&numsp;&numsp;`perWorkspace`</span> | object | PerWorkspace caps a single workspace. The effective cap is min(these, image.resources.max). |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`cpu`</span> | int-or-string | Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`home`</span> | int-or-string | Home caps the persistent home volume size. Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |
| <span className="crd-field-path">&numsp;&numsp;&numsp;&numsp;`memory`</span> | int-or-string | Pattern: `^(\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))(([KMGTPE]i)\|[numkMGTPE]\|([eE](\+\|-)?(([0-9]+(\.[0-9]*)?)\|(\.[0-9]+))))?$`. |

### spec.overrides

Overrides bounds instantiation-time template overrides for the
governed users, on top of each template's own allow-list.

| Field | Type | Description |
| --- | --- | --- |
| <span className="crd-field-path">`overrides`</span> | object |  |
| <span className="crd-field-path">&numsp;&numsp;`allowedFields`</span> | []string | AllowedFields the governed users may override when the template also allows them. |

### spec.subjects

Subjects this policy applies to (OR). Empty = every authenticated
user.

| Field | Type | Description |
| --- | --- | --- |
| <span className="crd-field-path">`subjects`</span> | []object |  |
| <span className="crd-field-path">&numsp;&numsp;`kind`</span> | string | SubjectKind says what a policy subject name refers to. **Required.** Allowed values: `User`, `Group`. |
| <span className="crd-field-path">&numsp;&numsp;`name`</span> | string | **Required.** |

